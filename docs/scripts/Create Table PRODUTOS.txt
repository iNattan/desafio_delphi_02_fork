CREATE TABLE PRODUTOS (
	ID INTEGER NOT NULL,
	NOME VARCHAR(100) NOT NULL,
	VALOR NUMERIC(15,2) NOT NULL,
	CONTROLE_ESPECIAL VARCHAR(1) NOT NULL,
	EXCLUIDO VARCHAR(1) DEFAULT 'N' NOT NULL,
	CONSTRAINT PRODUTOS_PK PRIMARY KEY (ID)
);
